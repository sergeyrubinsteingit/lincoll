{
  "name": "lincoll",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:8080/",
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.1.1",
    "node-modules": "^1.0.1",
    "react": "^18.1.0",
    "react-dom": "^18.0.0",
    "react-http-request": "^2.0.0",
    "react-scripts": "5.0.1",
    "rn-nodeify": "^10.3.0",
    "web-vitals": "^2.1.4",
    "write-json-file": "^5.0.0"
  },
  "scripts": {
    "start": "set PORT=8080 && react-scripts --max-http-header-size=8000 start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postinstall": "node_modules/.bin/rn-nodeify --install crypto,fs,https --hack"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 edge version",
      "last 1 firefox version",
      "last 1 chrome version",
      "last 1 safari version"
    ]
  }
}
